# Listener standard (MQTT)
listener 1883 0.0.0.0
protocol mqtt

# Listener per WebSockets
listener 9001 0.0.0.0
protocol websockets

# DEVI aggiungere questa riga per entrare senza password
allow_anonymous true